import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        width: '80%',
        marginTop: '15%',
        marginLeft: '10%',
    },
    taskListHeading:{
        textAlign: 'left',
        marginBottom: 35,
    },
    taskListText: {
        fontSize: 27,
        color: 'black',
        fontWeight: 'bold',
        letterSpacing: 1,
    }
});

export default styles;